import { defineConfig } from 'vite';

export default defineConfig({
  base: '/netflix-gpt/', // This is your GitHub repository name
  build: {
    outDir: 'dist', // Default output directory for Vite
  },
});
